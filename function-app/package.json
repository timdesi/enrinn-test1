{
    "name": "geojson-function-app",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rimraf dist",
        "prestart": "npm run clean && npm run build",
        "start": "func start"
    },
    "dependencies": {
        "@azure/functions": "^3.5.0"
    },
    "devDependencies": {
        "@types/node": "^16.x",
        "typescript": "^4.0.0",
        "rimraf": "^3.0.2"
    }
}
